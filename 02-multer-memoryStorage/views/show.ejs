<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% files.forEach(file => { %>
        <img style="width: 200px;" src="data:<%= file.mimetype %>;base64,<%= file.image.toString('base64') %>" >
    <% }); %>
</body>
</html>